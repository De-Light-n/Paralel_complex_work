# Asset Service - ConfigMap
# Configuration for database and Redis connections
apiVersion: v1
kind: ConfigMap
metadata:
  name: asset-service-config
  labels:
    app: asset-service
data:
  database_url: "postgresql+asyncpg://postgres:postgres@postgres-service:5432/asset_db"
  redis_url: "redis://redis-service:6379/0"
